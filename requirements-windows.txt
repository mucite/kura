# Kura v2026 — Windows Requirements
# 100% local, DSGVO-safe, no cloud services

# === Core ===
python-dotenv==1.0.0

# === GUI ===
# PySimpleGUI now requires private PyPI server
# Install with: py -3.12 -m pip install --extra-index-url https://PySimpleGUI.net/install PySimpleGUI
--extra-index-url https://PySimpleGUI.net/install
PySimpleGUI>=5.0.8

# === Audio (recording via sounddevice — no ffmpeg required) ===
sounddevice==0.4.6
numpy>=1.26.0

# === Document Generation ===
fpdf2>=2.7.8

# === Network (license + config sync) ===
requests>=2.31.0

# === System Monitoring ===
psutil>=5.9.0

# === AI/ML — Speech-to-Text (openai-whisper, 100% local) ===
openai-whisper>=20231117
# Local model stored in: models/whisper/large-v3.pt (~3GB)
# Model is bundled with the application for offline use

# === AI/ML — LLM (GGUF via llama-cpp-python, local) ===
# Pre-built wheel for Python 3.12 (Windows x64):
llama-cpp-python @ https://github.com/abetlen/llama-cpp-python/releases/download/v0.2.90/llama_cpp_python-0.2.90-cp312-cp312-win_amd64.whl

# === Build/Packaging ===
pyinstaller>=6.10.0
